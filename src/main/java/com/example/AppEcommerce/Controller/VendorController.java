package com.example.AppEcommerce.Controller;

/*import com.example.AppEcommerce.Dto.SignUpAdmin;
import com.example.AppEcommerce.Dto.VendorDto;
import com.example.AppEcommerce.Service.AuthService;
import com.example.AppEcommerce.Service.VendorService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import javax.validation.Valid;
import java.io.IOException;*/

/*@RestController
@RequestMapping("/vendor")
public class VendorController {
    @Autowired
    VendorService vendorService;
    @PostMapping(value="/registerVendor", consumes = {MediaType.MULTIPART_FORM_DATA_VALUE})//bech dakhelou des données wa m3ahom images
    public ResponseEntity<?> registerVendor(@RequestPart("vendor") VendorDto vendor, @RequestPart(name = "image", required = false) MultipartFile file) throws IOException {
        return vendorService.registerVendor(vendor,file);
    }
}
*/